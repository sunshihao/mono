# ---- 服务 ----
PORT=3002
LOG_LEVEL=info

# ---- 文件监听（chokidar + 轮询兜底；缺省则两者禁用，webhook 仍可用）----
INGEST_DIR=/path/to/docs

# ---- 统一写入的 Redis Stream（缺省则 XADD 跳过）----
REDIS_URL=redis://localhost:6379
POLL_INTERVAL_MS=60000
