# 后端 API 地址（默认本机 3000；跨机部署时改为网关地址）
NEXT_PUBLIC_API_URL=http://localhost:3000

# 会话签名密钥（服务端运行时注入，勿用 NEXT_PUBLIC_ 前缀；
# 生成：openssl rand -base64 32；Docker 部署时 docker run -e AUTH_SECRET=...）
AUTH_SECRET=
